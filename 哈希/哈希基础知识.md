
# 常见的三种哈希结构：数组、set（集合）、map（映射）

一般来说哈希表都是用来快速判断一个元素是否出现集合里**。

1.有效字母异位词

       for (int i = 0; i < s.length(); i++) {
            record[s.charAt(i) - 'a']++;     
           // 并不需要记住字符a的ASCII，只要求出一个相对数值就可以了
        }

        for (int i = 0; i < t.length(); i++) {
            record[t.charAt(i) - 'a']--;
        }

2.四数相加

3.三数之和：双指针法：要求返回的是数据，不是位置

  先排序、循环i=0开始，left = i+1；right = nums.length - 1

考虑去重：nums[i] == nums[i - 1] 去重


# set
Set<Integer> set = new HashSet<>();
Set<String> set = new HashSet<>(Arrays.asList(1,2,3));

set.add(1)
set.contains(key);

for(Integer i : set){
    //set内的值
    i;
    System.out.println(i);
}

HashSet底层是hash表，不保证元素的迭代顺序
LinkedHashSet 可以按照插入顺序输出

set.size()

# HashMap
HashMap<Integer，String> map = new HashMap<>();
HashMap<Integer,String> map = new HashMap<>(){{put(1,"a");put(2,"b")}}

mao.containsKey(key);
map.containsValue(value) ；需要遍历获取
map.getOrDefault(key,0);
map.putIdAbsent(key,"default");

for(int key : map.keySet()){

}
for(String value : map.values()){

}

for(Map.Entry<Integer,String> entry : map.entrySet()){
      int key = entry.getKey();
      String val = entry.getValue();
}


